<? include("box_header.phtml") ?>
<div class="inner">
<div class="titre textalia textsizec">Mes Comp&eacute;tences m&eacute;tiers</div>
 <div class="contenuB">
  <div class="box_contenu1 textalic">
 
 <? 
 
if ($this->user->activation == false) :
 	echo "Tour non activé à la connexion";
elseif ($this->affichageInterne == true) : ?>
	 <?php if (count($this->competences) > 0) : ?>
	 
		<ul id="navigation" style="width:100%;">
		<? foreach($this->competences as $m) : ?>
			<li id="menuCompetencesMetier<?=$m["nom_systeme_metier"]?>">
				<? if (count($this->competences) > 2): $display = "none"; $css = "head"; else: $display = "block"; $css = "head selected"; endif;?>
				<h3 id="menuCompetencesMetier<?=$m["nom_systeme_metier"]?>-head" class="<?=$css?>" onclick="maccordion(this);"><?=$m["nom_metier"]?></h3>
				<? if (count($this->competences) > 2): $display = "none"; else: $display = "block"; endif;?>
				<ul id="menuCompetencesMetier<?=$m["nom_systeme_metier"]?>-body" style="display:<?=$display?>">
				<? foreach($m["competences"] as $c) : ?>
					<li>
					<? if ($this->user->pa_braldun >=$c["pa_utilisation"]) : 
				      $attr = "onclick=\"Javascript:_get_('/competences/doaction?caction=ask_competence_".$c["nom_systeme"]."')\"";
				    else:
				      $attr = " disabled ";
				    endif;?>
				    <input type="button" class='button1' value="<?=$c["pa_texte"]?> PA - <?=$c["nom"]?><? if ($c["pourcentage_init"] < 100) : echo " ".$c["pourcentage"]." %"; endif; ?>" <?=$attr?>>
			    
					</li>
				<? endforeach; ?>
				</ul>
			</li>
		<? endforeach; ?>
		</ul>
	 
	 <?php 
	  else :	
	 	echo "Aucune comp&eacute;tence m&eacute;tier";
	  endif;
	 ?>
 
<? else: ?>
	Chargement en cours
	<br /><img src="<?=$this->config->static->url?>/images/wait.gif" />
<? endif; ?>

 </div>
 </div>
</div>
<? include("box_bottom.phtml") ?>