 <? foreach ($this->typePlantes["Fleur"]["type_plante"] as $k => $v) : ?>
 	<? if ($v["a_afficher"] == true) : ?>
 	<tr class="table_liste">
	 	<td class="table_liste" style="text-align: left;">
	 		<?=htmlspecialchars($k); ?>
	 	</td>
	 	<td class="table_liste">
	 		<?=Bral_Helper_Contenu::affichePlante($v["parties"]["fleur"]); ?>
	 	</td>
 	</tr>
 	<? endif; ?>
 <? endforeach; ?>
