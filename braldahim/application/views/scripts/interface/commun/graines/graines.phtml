<? 
Zend_Loader::loadClass('Bral_Helper_Contenu');
?>
<? if (count($this->graines) == 0) : ?>
   <center><i>vide</i></center>
<? else : ?>
    <table class="table_liste" style="width:auto; padding:5px;" >
		<thead class="table_liste">
		  <tr class="table_liste">
		  	<? foreach ($this->graines as $g) :  ?>
			<th style="width:auto; padding:5px;" ><?=htmlspecialchars($g["type"])?></th>
			<? endforeach; ?>
		  </tr>
		</thead>
		<tbody>
			<tr class="table_liste">
			<? foreach ($this->graines as $g) : ?>
			 	<td class="table_liste">
			 		<?=Bral_Helper_Contenu::afficheGraine($g)?>
			 	</td>
			<? endforeach; ?>
			</tr>
		</tbody>
	</table>
<? endif; ?>
<br />