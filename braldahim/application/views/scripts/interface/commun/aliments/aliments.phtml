<? Zend_Loader::loadClass("Bral_Helper_DetailAliment"); ?>

<table class="table_liste" style="width:90%"  <? if ($this->idAlimentsTable != false) : ?> id="<?=$this->idAlimentsTable?>"<? endif; ?>>
	<thead class="table_liste">
	  <tr class="table_liste">
	  	<th>N&deg;</th>
	  	<th>&nbsp;</th>
		<th>Nom</th>
		<th>Qualit&eacute;</th>
		<th>Type de Recette</th>
		<th>Bonus sur la Balance de Faim</th>
		<th>Poids</th>
		<? if ($this->estElementsEtal === true) : ?>
			<th>Prix</th>
			<th>Commentaire du vendeur</th>
		<? endif; ?>
		<? if ($this->estElementsEtalAchat === true) : ?>
			<th>Marchander / Acheter</th>
		<? endif; ?>
	  </tr>
	</thead>
	<tbody>
		<? if (count($this->aliments) > 0) :?>
			<? $i = 0;
			   foreach($this->aliments as $a) :?>
				  <tr class="table_liste<?=($i % 2);$i++; ?>">
			  		<td class="table_liste"><?=$a["id_aliment"] ?></td>
			  		<td class="table_liste"><img src='/public/styles/braldahim_defaut/images/type_aliment/type_aliment_<?=$a["id_type_aliment"]?>.png' alt="<?=htmlspecialchars($a["nom"])?>"/></td>
			  		<td class="table_liste"><?=htmlspecialchars($a["nom"]) ?></td>
			  		<td class="table_liste">
				  		<? if ($a["bbdf"] > 0): ?>
				   		<?=htmlspecialchars($a["qualite"]) ?>
				   		<? else:?>
				   			n.a
				   		<? endif;?>
			   		</td>
			  		<td class="table_liste"><?=htmlspecialchars($a["recette"]) ?></td>
			  		<td class="table_liste">
			  		
			   		<? if ($a["bbdf"] > 0): ?>
			  			<?=$a["bbdf"] ?> %
			  		<? else: ?>
			  			n.a
			  		<? endif; ?> </td>
			  		<td class="table_liste"><?=Bral_Util_Poids::POIDS_ALIMENT ?> Kg</td>
			  		<? if ($this->estElementsEtal === true) : ?>
						<td class="table_liste" nowrap><?=Bral_Helper_DetailAliment::afficherPrix($a);?></td>
						<td class="table_liste"><?=Bral_Util_BBParser::bbcodeReplace($a["commentaire_vente_echoppe_aliment"])?></td>
					<? endif; ?>
					<? if ($this->estElementsEtalAchat === true) : ?>
						<td class="table_liste">
						<input type="button" class='button1' onClick='_get_("/echoppe/doaction?caction=ask_echoppe_acheteraliment&valeur_1=<?=$a["id_aliment"] ?>");' value="<?=$this->config->game->echoppe->nb_pa_service ?> PA - Acheter">
						</td>
					<? endif; ?>
				  </tr>
			<? endforeach; ?>
		<? else: ?>
			<tr class="table_liste"><td class="table_liste" colspan="20"><i>aucun aliment</i></td></tr>
		<? endif; ?>
	</tbody>
</table>