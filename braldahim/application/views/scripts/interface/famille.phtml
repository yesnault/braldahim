<? include("box_header.phtml") ?>
<div class="inner">
<div class="titre textalia textsizec">Ma Famille</div>
 <div class="contenuC">
  <div class="box_contenu1 textalic">
  
 <? if ($this->affichageInterne == true) : ?>
 
 <table>
	<tr>
	<td><img src='/public/styles/braldahim_defaut/images/vue/famille.png'"/></td>
	
	<td>
		<? if ($this->pereMereOk === true) :?>
	  		Suite &agrave; l'heureuse rencontre entre votre m&egrave;re 
	  		<a title="Voir le profil" onclick="javascript:ouvrirWin('/voir/braldun/?braldun=<?=$this->user->id_fk_mere_braldun ?>');">
	  		<? if ($this->mere == null): ?>
	  			Braldun inconnue (n&deg;<?=$this->user->id_fk_mere_braldun ?>)
	  		<? else : ?>
	  			<? if ($this->mereAncienne) : ?>
	  				<?=$this->mere->prenom_ancien_braldun ?> <?=$this->mere->nom_ancien_braldun ?>
	  			<? else: ?>
	  				<?=$this->mere->prenom_braldun ?> <?=$this->mere->nom_braldun ?>
	  			<? endif;?>
	  			(n&deg;<?=$this->user->id_fk_mere_braldun ?>)
	  		<? endif;?>
	  		</a>
		  	et votre p&egrave;re
	  		<a title="Voir le profil" onclick="javascript:ouvrirWin('/voir/braldun/?braldun=<?=$this->user->id_fk_pere_braldun ?>');">
	  		<? if ($this->pere == null): ?>
	  			Braldun inconnu (n&deg;<?=$this->user->id_fk_pere_braldun ?>)
	  		<? else : ?>
	  			<? if ($this->pereAncien) : ?>
	  				<?=$this->pere->prenom_ancien_braldun ?> <?=$this->pere->nom_ancien_braldun ?>
	  			<? else: ?>
	  				<?=$this->pere->prenom_braldun ?> <?=$this->pere->nom_braldun ?>
	  			<? endif;?>
	  			(n&deg;<?=$this->user->id_fk_pere_braldun ?>)
	  		<? endif;?>
	  		</a>
		    		
	  	vous &ecirc;tes n&eacute;<? if ($this->user->sexe_braldun == 'feminin') echo "e"?> le <?=$this->dateNaissance ?>.
	  	
	  	<? else: ?>
	  	Vous &ecirc;tes n&eacute;<? if ($this->user->sexe_braldun == 'feminin') echo "e"?> orphelin<? if ($this->user->sexe_braldun == 'feminin') echo "e"?> le <?=$this->dateNaissance ?>
	  	<? endif; ?>
	    <br /><br />
	
		<? if ($this->nbEnfants == 0 && $this->conjoint == null) :?>
	    	Vous n'avez ni enfant, ni conjoint<? if ($this->user->sexe_braldun != 'feminin') echo "e"?>.
	    <? else : ?>
	    	Avec votre conjoint<? if ($this->user->sexe_braldun != 'feminin') echo "e"?>
	    	<? if ($this->conjoint != null) : ?>
		    	<a title="Voir le profil" href="#" onclick="javascript:ouvrirWin('/voir/braldun/?braldun=<?=$this->conjoint["id_braldun"] ?>');">
		    		<?=$this->conjoint["prenom"] ?> <?=$this->conjoint["nom"] ?> (n&deg;<?=$this->conjoint["id_braldun"] ?>)</a>,
		    <? else: ?>
		    	disparu<? if ($this->user->sexe_braldun != 'feminin') echo "e"?>
		    <? endif; ?>
	    	 vous avez eu <?=$this->nbEnfants ?> enfant<? if ($this->nbEnfants > 1) echo 's' ?>.<br />
	    <? endif; ?>
	</td>
	</tr>
	</table>
	
	<? if ($this->nbEnfants > 0) : ?>
    	<table class="table_liste" style="width:80%">
			<thead class="table_liste">
			  <tr class="table_liste" title="Cliquer pour Trier">
				<th>Braldun</th>
				<th>Naissance</th>
			  </tr>
			</thead>
			<tbody>
				<? foreach($this->enfants as $e) :?>
				<tr class="table_liste">
					<td><a title="Voir le profil" href="#" onclick="javascript:ouvrirWin('/voir/braldun/?braldun=<?=$e["id_braldun"] ?>');"><?=$e["prenom"] ?> <?=$e["nom"] ?> (n&deg;<?=$e["id_braldun"]?>)</a></td>
					<td nowrap>n&eacute;<? if ($e["sexe_braldun"] == 'feminin') echo "e"?> le <?php echo Bral_Util_ConvertDate::get_datetime_mysql_datetime('d/m/y',$e["date_naissance"] );?></td>
				</tr>
				<? endforeach; ?>
			</tbody>
		</table>
	<? endif; ?>
	
    
    
    
   <br />
   
 <? else: ?>
	Chargement en cours
	<br /><img src="/public/images/wait.gif" />
<? endif; ?>
   </div>
</div>
</div>
<? include("box_bottom.phtml") ?>