<?php if ($this->affichageInterne == false) : ?>
<div class="<?=$this->nom_interne ?>" id="<?=$this->nom_interne ?>" style="display:<?=$this->display; ?>">
<?php endif; ?>
 <div class="box_titre1">
   Laban </div>
 <div class="box_contenu2">
   Castars : <?=$this->user->castars_hobbit ?><br>
   <hr>
   <div class="laban_plante">
   <? if ($this->nb_plantes == 0) : ?>
   	Aucune plante
   <? else : ?>
    <? if ($this->nb_plantes==1):$s=''; else:$s='s';endif; ?>
    <?=$this->nb_plantes?> plante<?=$s?> : <br>
    
    <table width='100%'>
    <? $i = 0;
      foreach ($this->plantes as $p) : 
    	 
    	 if ($i % 3 == 0) echo "<tr>"; 
    	 
    	$textPlante = "Cat&eacute;gorie : ".htmlentities($p["categorie"]). "<br>"; 
    	$textPlante .= "Nom : ".htmlentities($p["type"])." \(".$p["id_laban_plante"]."\)<br>";
    	$textPlante .= " Ce qu\'elle porte sur elle : <br>";
    	$textPlante .= htmlentities($p["nom_partie_1"]). ", quantit&eacute; : ".$p["partie_1"]; 
    	if ($p["nom_partie_2"] != "") {
    		$textPlante .= htmlentities($p["nom_partie_2"]). ", quantit&eacute; : ".$p["partie_2"]; 
    	}
    	if ($p["nom_partie_3"] != "") {
    		$textPlante .= htmlentities($p["nom_partie_3"]). ", quantit&eacute; : ".$p["partie_3"]; 
    	}
    	if ($p["nom_partie_4"] != "") {
    		$textPlante .= htmlentities($p["nom_partie_4"]). ", quantit&eacute; : ".$p["partie_4"]; 
    	}
    	?>
    	<td onmouseover="return overlib('<?=$textPlante?>',CAPTION,'<?=htmlentities($p["type"])?>\(<?=$p["id_laban_plante"]?>\) - D&eacute;tails');"
      		onclick="return overlib('<?=$textPlante?>', STICKY, CAPTION, 'Plante <?=htmlentities($p["type"])?>\(<?=$p["id_laban_plante"]?>\) - D&eacute;tails', CLOSECLICK, EXCLUSIVE);"
      		onmouseout="return nd();">
    		<?=$p["categorie"]?> : <?=$p["type"]?> (<?=$p["id_laban_plante"]?>)
    	</td>
    	<? if ($i % 3 == 2) echo "</tr>"; 
    	$i++; 
     endforeach;
     if  ($i % 3 != 0) echo "</tr>"; 
     ?>
    </table>
   <? endif; ?>
   </div>
   
   <div class="laban_minerai">
   <? if ($this->nb_minerais == 0) : ?>
   	Aucun minerai
   <? else : ?>
    <? if ($this->nb_minerais==1):$s=''; else:$s='s';endif; ?>
    <?=$this->nb_minerais?> minerai<?=$s?> : <br>
    
    <table width='100%'>
    <? $i = 0;
      foreach ($this->minerais as $m) : 
     	 if ($i % 3 == 0) echo "<tr>"; 
    	?>
    	<td>
    		<?=$m["type"]?> : Quantit&eacute; : <?=$m["quantite"]?>
    	</td>
    	<? if ($i % 3 == 2) echo "</tr>"; 
    	$i++; 
     endforeach;
     if  ($i % 3 != 0) echo "</tr>"; 
     ?>
    </table> 
    <? endif; ?>
   </div>
   <br>
</div>
 <?php if ($this->affichageInterne == false) : ?>
</div>
<?php endif; ?>