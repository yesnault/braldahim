<?php if ($case["n_elements_potions"] > 0) : 
	$textElementsPotions = "";
	foreach($case["elements_potions"] as $p) {
		$textElementsPotions .= $p["nom_type"]. " ".htmlspecialchars($p["nom"]) ." de qualit&eacute; ".htmlspecialchars($p["qualite"]) ." (n&deg; ".$p["id_element_potion"].") <br />";
	} 
	$s = "";
	if ($case["n_elements_potions"] > 1) {
		$s = "s";
	}
endif; ?>