     	<?php if ($case["n_elements_runes"] > 0) : ?>
      		<div>
      		<?php 
      			$textRune = "";
      			foreach($case["elements_runes"] as $r) {
      				$textRune .= "1 rune (n&deg; ".$r["id_element_rune"].")<br />";
				} 
				$s = "";
				if ($case["n_elements_runes"] > 1) {
					$s = "s";
				}
				$titre = "Position X:".$case["x"]." Y:".$case["y"]." - ".$case["n_elements_runes"]."  Rune".$s;
			?>
      		<span <?=Bral_Helper_Tooltip::jsTip($textRune, $titre) ?>>
	      		<?=$case["n_elements_runes"] ?> rune<?=$s?>
	      	</span>
	      	</div>
	    <?php endif; ?>