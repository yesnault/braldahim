
<? if ($this->venteDefaut == true) : ?>
	<ul class="titre textalic">
	Affichage des 50 dernières ventes inscrites
	</ul>
	Vous pouvez affiner votre recherche avec les listes à choix présentes ci-dessus.
	<br><br>
<? endif;?>
 <br>
 <table class="table_liste" style="width:95%">
	<thead class="table_liste">
	  <tr class="table_liste">
	  	<th>N&deg; Vente <br>Date fin</th>
		<th>Nom</th>
		<th>Vendeur</th>
		<th>Prix</th>
		<th>Commentaire</th>
		<th>Acheter</th>
	  </tr>
	</thead>
  <? if (count($this->resultats) > 0): ?>
	<tbody>
	
	<? if ($this->user->pa_hobbit >= $this->paUtilisationHotel): 
		$disabled = "";
	else:
		$disabled = "disabled" ;
	endif;
	?>

  	<? $i = 0; 
  	foreach($this->resultats as $e): ?>
  		<tr class="table_liste<?=($i % 2);$i++; ?>">
  			<td class="table_liste">N&deg;<?=$e["vente"]["id_vente"]?>
  			<br><?=$e["vente"]["date_fin_vente"]?>
  			</td>
	  		<td class="table_liste">
	  		<?
	  		if ($e["type"] == "equipement") :
	  			include("resultats_equipements.phtml");
	  		elseif ($e["type"] == "munition") :
	  			include("resultats_munitions.phtml");
	  		elseif ($e["type"] == "element") :
	  			include("resultats_elements.phtml");
	  		elseif ($e["type"] == "minerai") :
	  			include("resultats_minerais.phtml");
	  		elseif ($e["type"] == "partieplante") :
	  			include("resultats_plantes.phtml");
	  		elseif ($e["type"] == "graine") :
	  			include("resultats_graines.phtml");
	  		elseif ($e["type"] == "ingredient") :
	  			include("resultats_ingredients.phtml");
	  		elseif ($e["type"] == "aliment") :
	  			include("resultats_aliments.phtml");
	  		elseif ($e["type"] == "potion") :
	  			include("resultats_potions.phtml");
	  		elseif ($e["type"] == "rune") :
	  			include("resultats_runes.phtml");
	  		elseif ($e["type"] == "materiel") :
	  			include("resultats_materiels.phtml");
	  		endif;
	  		?>
	  		</td>
	  		<td class="table_liste">
	  			<a onClick="javascript:ouvrirWin('/voir/hobbit/?hobbit=<?=$e["vente"]["id_hobbit"]?>');" title="Cliquez pour voir le profil">
		  		<?=$e["vente"]["prenom_hobbit"]?> <?=$e["vente"]["nom_hobbit"]?> (<?=$e["vente"]["id_hobbit"]?>)
		  		</a>
		  		<img src='/public/images/uddeim/env.gif' title='Envoyer un message' alt='Ecrire' border='0' onClick="ecrireMessage('<?=$e["vente"]["id_hobbit"]?>');" style='cursor:pointer'/>
	  		</td>
	  		<td class="table_liste">
	  		<?=Bral_Helper_DetailHotel::afficherPrix($e["vente"]);?>
	  		</td>
	  		<td class="table_liste"><?=Bral_Util_BBParser::bbcodeReplace($e["vente"]["commentaire_vente"])?></td>
	  		<td class="table_liste">
	  			<input type="button" class='button1' onClick='_get_("/hotel/doaction?caction=ask_hotel_acheter&valeur_1=<?=$e["vente"]["id_vente"]?>");' value="<?=$this->paUtilisationHotel ?> PA - Acheter" <?=$disabled?>>
		  	</td>
  		</tr>
  	<? endforeach; ?>
  	</tbody>
   <? else: ?>
   	<tr class="table_liste">
   		<td colspan="5">
   			Aucun r&eacute;sultat
   		</td>
   	</tr>
   <? endif; ?>
</table>
