 <br>
 <table class="table_liste" style="width:90%">
	<thead class="table_liste">
	  <tr class="table_liste">
	  	<th>N&deg; Vente</th>
		<th>Nom</th>
		<th>Vendeur</th>
		<th>Localisation</th>
		<th>Prix</th>
		<th>Acheter</th>
	  </tr>
	</thead>
  <? if (count($this->resultats) > 0): ?>
	<tbody>
  	<? $i = 0; 
  	foreach($this->resultats as $e): ?>
  		<tr class="table_liste<?=($i % 2);$i++; ?>">
  			<td class="table_liste"><?=$e["vente"]["id_vente"]?></td>
	  		<td class="table_liste">
	  		<?
	  		if ($e["type"] == "equipement") :
	  			include("resultats_equipements.phtml");
	  		elseif ($e["type"] == "element") :
	  			include("resultats_elements.phtml");
	  		elseif ($e["type"] == "aliment") :
	  			include("resultats_aliments.phtml");
	  		elseif ($e["type"] == "potion") :
	  			include("resultats_potions.phtml");
	  		elseif ($e["type"] == "rune") :
	  			include("resultats_runes.phtml");
	  		endif;
	  		
	  		?>
	  		</td>
	  		<td class="table_liste">
	  			<a onClick="javascript:ouvrirWin('/voir/hobbit/?hobbit=<?=$e["vente"]["id_hobbit"]?>');" title="Cliquez pour voir le profil">
		  		<?=$e["vente"]["prenom_hobbit"]?> <?=$e["vente"]["nom_hobbit"]?> (<?=$e["vente"]["id_hobbit"]?>)
		  		</a>
		  		<img src='/public/images/uddeim/env.gif' title='Envoyer un message' alt='Ecrire' border='0' onClick="ecrireMessage('<?=$e["vente"]["id_hobbit"]?>');" style='cursor:pointer'/>
	  		</td>
	  		<td class="table_liste">En Stock</td>
	  		<td class="table_liste">
	  		<?=Bral_Helper_DetailHotel::afficherPrix($e["vente"]);?>
	  		</td>
	  		<td class="table_liste"></td>
  		</tr>
  	<? endforeach; ?>
  	</tbody>
   <? else: ?>
   	<tr class="table_liste">
   		<td colspan="5">
   			Aucun r&eacute;sultat
   		</td>
   	</tr>
   <? endif; ?>
</table>
