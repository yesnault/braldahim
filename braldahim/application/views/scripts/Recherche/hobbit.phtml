<ul class="resultats">
<? if (count($this->tabHobbits) == 0): ?>
	<li class="resultat">Aucun Hobbit</li>
<? elseif (count($this->tabHobbits) > 25): ?>
	<li class="resultat">Trop de hobbits !</li>
<? else: ?>
	<?php foreach ($this->tabHobbits as $h) : ?>
		<li class="resultat" id_fk_jos_users_hobbit='<?=$h["id_fk_jos_users_hobbit"]?>' id_hobbit='<?=$h["id_hobbit"]?>' champ='<?=$this->champ?>' valeur='<?=$h["prenom"]?> <?=$h["nom"]?>'><?
			$pos = stripos($h["nom"], $this->pattern);
			$nom = mb_substr($h["nom"], 0, $pos) . '<font color="red">'.mb_substr($h["prenom"], $pos , mb_strlen($this->pattern)) .'</font>'.mb_substr($h["prenom"], $pos + mb_strlen($this->pattern));
			$nom .= " ".$h["nom"];
			echo $nom ." (".$h["id_hobbit"].")" ?></li>
	<?php endforeach; ?>
<? endif; ?>
</ul>