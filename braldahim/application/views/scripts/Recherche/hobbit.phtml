<ul class="resultats">
<? if (count($this->tabHobbits) == 0): ?>
	<li class="resultat">Aucun Hobbit</li>
<? elseif (count($this->tabHobbits) > 25): ?>
	<li class="resultat">Trop de hobbits !</li>
<? else: ?>
	<?php foreach ($this->tabHobbits as $h) : ?>
		<li class="resultat" id_hobbit="<?=$h["id_hobbit"]?>" champ="<?=$this->champ?>" valeur="<?=$h["prenom"]?> <?=$h["nom"]?>"><?
			$pos = stripos($h["prenom"], $this->pattern);
			$nom = mb_substr($h["prenom"], 0, $pos) . '<font color="red">'.mb_substr($h["prenom"], $pos , mb_strlen($this->pattern)) .'</font>'.mb_substr($h["prenom"], $pos + mb_strlen($this->pattern));
			$nom .= " ".$h["nom"];
			echo $nom ." (".$h["id_hobbit"].")" ?></li>
	<?php endforeach; ?>
<? endif; ?>
</ul>