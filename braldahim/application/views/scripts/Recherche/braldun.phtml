<?php 
echo "[";

if (count($this->tabBralduns) == 0):
	echo '{ "id": "-1", "label": "Pas de resultat", "value": "Pas de resultat" }';
elseif (count($this->tabBralduns) > 25):
	echo '{ "id": "-2", "label": "Trop de resultat", "value": "Trop de resultat" }';
else:
	echo $this->resultat;
endif;
echo ']';