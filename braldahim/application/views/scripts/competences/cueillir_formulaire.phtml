 <input type='hidden' id='nb_valeurs' name='nb_valeurs' value='2'>
 <div class='comp_contenu'>
 
 	<? if ($this->planteOk === true) : ?>
	 Pour <?=$this->nb_pa ?> PA, vous pouvez cueillir des &eacute;l&eacute;ments d'une plante...<br /><br />
	 
	 <select name="valeur_1" id="valeur_1"  onchange="javascript:if ($('valeur_1').value==-1){$('bouton_cueillir').disabled=true } else {$('bouton_cueillir').disabled=false }">
	 	<option value="-1">(Choisissez une plante)</option>
	 	<? foreach($this->plantes as $p) : ?>
	 		<option value="<?=$p["id_plante"]?>"><?=$p["categorie"]?> - <?=$p["nom_type"]?> (<?=$p["id_plante"]?>)</option>
	 	<? endforeach; ?>
	 </select>
	 <br /><br />
 	 <? if ($this->charettePleine == true && $this->possedeCharrette == true) : ?>
 		Votre charette ne peut plus contenir de &eacute;l&eacute;ment.<br />
 	 <? endif;?>
 	 <? if ($this->labanPlein == true) : ?>
 		Votre laban ne peut plus contenir de &eacute;l&eacute;ment.<br />
 	 <? endif;?>
 	 <br />
 	 Choisissez la destination :
	 <br />
	 <?$select="";?>
	 <select name="valeur_2" id="valeur_2">
		<? if ($this->labanPlein == false ) : ?>
			<? $select = " selected='selected' ";?>
			<option value="2" <?=$select;?>>Votre laban (<?=$this->nbPossibleDansLabanMax?> &eacute;l&eacute;ment(s) maximum)</option>
		<? endif; ?>
		<? if ( $this->possedeCharrette == true && $this->charettePleine == false ) : ?>
			<? if ($select == "") $select = " selected='selected' "; else $select="";  ?>
			<option value="1" <?=$select;?>>Votre charrette (<?=$this->nbPossibleDansCharretteMax?> &eacute;l&eacute;ment(s) maximum)</option>		
		<? endif; ?>
		<option value="3">Le sol</option>
	 </select>
	 <? else: ?>
	 Il n'y a pas de plante ici...
	 <? endif; ?>
</div>

<br />
<?php if ($this->assezDePa == true && $this->planteOk === true ) : ?>
<input type='button' class='button1' id='bouton_cueillir' value='Cueillir !' onclick='this.disabled=true;_get_("/competences/doaction?caction=do_competence_cueillir");' disabled>
<? endif ?>
