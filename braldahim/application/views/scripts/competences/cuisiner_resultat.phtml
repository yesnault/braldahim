<? include("commun_text_resultat.phtml") ?>
		
<? if ($this->okJet1 === true):
	$s = "";
	if ($this->nbAliment > 1) : $s = "s"; endif;
?>
	Vous avez r&eacute;ussi votre cuisine.<br /> <br />
	
	<? if ($this->estQuintuple === true) : ?>
		Les hobbits suivants se sont régalés avec votre banquet : <br>
		<? foreach($this->hobbits as $h) : ?>
			<? if ($h['id_hobbit'] == $this->user->id_hobbit) : ?>
				Vous-même
			<? else: ?>
				<label class='alabel profil' onClick="ouvrirProfilH('<?=$h["id_hobbit"]?>');" title='Voir le profil' >
			<?=$h["prenom_hobbit"]?> <?=$h["nom_hobbit"]?> (n&deg; <?=$h["id_hobbit"]?>)
			</label>
			<? endif;?>
			<br>
		<? endforeach;?>
	<? else: ?>
		Vous avez prépraré <?=$this->nbAliment ?> part<?=$s?> de 
		<?=$this->typeAlimentCourant["nom_type_aliment"]?> de qualit&eacute; <?=$this->qualite?> avec pour effet sur la balance de +<?=$this->bbdfAliment?> %.
		<br />
		<? if ($this->nbAlimentATerre > 0): ?>
			Faute de place, <?=$this->nbAlimentATerre ?> part<? if ($this->nbAlimentATerre > 1): echo "s sont tomb&eacute;es terre"; else: " est tomb&eacute;e à terre"; endif?>.
		<? endif;?>
		<br /><br />
		
		<? if ($this->effetRune) :?>
			Une rune que vous portez a eu de l'effet sur le nombre d'aliment pr&eacute;par&eacute; : + 1 part.
		<? endif; ?>
	<? endif;?>
	
	<? if ($this->recetteAvecPotion === true) : ?>
		 La potion utilisée (potion n°<?=$this->idPotion?>) sera appliquée lors de la consommation de la préparation.<br />
	<? endif;?>
<? else: ?>
	Vous avez manqu&eacute; votre cuisine. Vous avez perdu la moitié des ingrédients nécessaires.<br />
<? endif; ?>