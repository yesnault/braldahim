<ul class="titre textalic textsizec">Construction des champs</ul>
 <div class="contenuc">
  <ul class="box_contenu1 textalic">
  <br />
  	
  	Pour obtenir un champ, rendez-vous dans &agrave; l'<a title="Cliquez pour voir l''Office Notariale dans les règles" style="text-decoration: none" onClick="javascript:ouvrirWin('http://wiki.braldahim.com/wiki/Office_Notarial')";>Office Notarial</a>.<br /> 
  	Vous ne pourrez obtenir de champ que dans la Comt&eacute; o&ugrave; vous 
  	serez. <br />
 	
 	<br />
	<? if ($this->nChamps < 1) : ?>
  		Vous n'avez aucun champ.<br /><br />
   	<? else: ?>
   	
   	<table class="table_liste">
		<thead class="table_liste">
		  <tr class="table_liste">
			<th>Comté</th>
			<th colspan="<?=$this->nChamps?>">Position de vos champs [x ; y]</th>
		  </tr>
		</thead>
		<tbody>
		<? $i = 0;
		   $champ = false;
		   foreach($this->tabRegions as $r) : ?>
			
			<tr class="table_liste<?=($i % 2);$i++; ?>">
				<td class="table_liste">
				<?=htmlspecialchars($r["nom_region"]); ?>
				</td>
				
				<? if ($r["champs"] != null) :?>
			   		<? foreach($r["champs"] as $c) :?>
		   				<td class="table_liste" style="cursor:pointer" onClick="_get_('/champs/doaction?caction=ask_champs_voir&valeur_1=<?=$c["id_champ"]?>')">
		   					<?=$c["nom_champ"] ?> <span title="Cliquez pour regarder le contenu du champ"  class="alabel">[<?=$c["x_champ"] ?> ; <?=$c["y_champ"] ?>]
		   				</td>
			   		<? endforeach; ?>
		   		<? else: ?>
		   			<td class="table_liste" colspan="<?=$this->nChamps?>">&nbsp;</td>	
		   		<? endif; ?>
			</tr>
		<? endforeach; ?>
		</tbody>
	</table>
	<? endif; ?>
	<br /><br />
   </ul>
</div>
