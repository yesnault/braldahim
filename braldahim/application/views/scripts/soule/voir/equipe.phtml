
<table class="table_liste" style="width:100%">
	<thead class="table_liste">
	  <tr class="table_liste">
		<th><?=$equipe["nom_equipe"]?></th>
	  </tr>
	</thead>
	<tbody>
		<? if ($equipe["joueurs"] != null && count($equipe["joueurs"]) > 0): ?>
		  	<? 
		  	$i = 0; 
		  	foreach($equipe["joueurs"] as $j): ?>
		  		<tr class="table_liste<?=($i % 2);$i++; ?>">
			  		<td class="table_liste" nowrap>
			  		<a onClick="javascript:ouvrirWin('/voir/hobbit/?hobbit=<?=$j["id_hobbit"]?>');" title="Cliquez pour voir le profil">
			  		<?=$j["prenom_hobbit"]?> <?=$j["nom_hobbit"]?> (<?=$j["id_hobbit"]?>)
			  		</a>
		  		</tr>
		  	<? endforeach; ?>
		<? else: ?>
		Aucun joueur
		<? endif; ?>
  	</tbody>
</table>