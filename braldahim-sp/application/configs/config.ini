[general]
db.game.adapter = PDO_MYSQL
db.game.config.host = localhost
db.game.config.username = braldahim_jeu
db.game.config.password = braldahim_jeu
db.game.config.dbname = braldahim_jeu

/* actif: 1, sinon 0 */
tracker.enabled=0
tracker.id=UA-4252981-2

layout.layout     = template
layout.contentKey = contenu
layout.layoutPath = ../application/views/layouts/

static.url=http://braldahim-static.local

/* oui ou non : affiche les logs a l'ecran */
log.general.debug_browser = non;

log.fichier.scripts = /Users/yvonnickesnault/Projets/braldahim/logs/bral_scripts.log

/*
 *   const EMERG   = 0;  // Emergency: system is unusable
 *   const ALERT   = 1;  // Alert: action must be taken immediately
 *   const CRIT    = 2;  // Critical: critical conditions
 *   const ERR     = 3;  // Error: error conditions
 *   const WARN    = 4;  // Warning: warning conditions
 *   const NOTICE  = 5;  // Notice: normal but significant condition
 *   const INFO    = 6;  // Informational: informational messages
 *   const DEBUG   = 7;  // Debug: debug messages
 *   TRACE = 8; => + de debug encore
 */
log.niveau.scripts = 8;

game.x_min = -800;
game.x_max = 800;
game.y_min = -500;
game.y_max = 500;

general.mail.exception.from = exception@braldahim.com
general.mail.exception.nom = Braldahim-Exception-SP
/* envoi mail sur exception : actif : 1, sinon 0. */
general.mail.exception.use = 0
