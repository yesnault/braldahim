<?php echo $this->render("commun/box_header.phtml") ?>
<div class="inner">
<div class="box_contenu1 textalic">
	<?php include("filtre.phtml") ?>
	
	<br />
	Les runes découvertes ce mois-ci ne sont pas prises en compte.<br />
	<br />
	<table class="table_liste" style="width:90%">
	<thead class="table_liste">
	  <tr class="table_liste">
		<th>Catégorie</th>
		<th>Nombre</th>
	  </tr>
	</thead>
  	<?php if (count($this->categories) > 0): ?>
		<tbody>
	  	<?php $i = 0; 
	  	foreach($this->categories as $f): ?>
	  		<tr class="table_liste<?php echo ($i % 2);$i++; ?>">
		  		<td class="table_liste">
		  			<?php if ($f["niveau"] == "d"): ?>
		  				Runes communes 
		  			<?php elseif ($f["niveau"] == "c"): ?>
		  				Runes peu communes
		  			<?php elseif ($f["niveau"] == "b"): ?>
		  				Runes rares 
		  			<?php elseif ($f["niveau"] == "a"): ?>
		  				Runes très rares 
		  			<?php endif;?>
		  		</td>
		  		<td class="table_liste">
		  			<?php echo $f["nombre"]?>
		  		</td>
	  		</tr>
	  	<?php endforeach; ?>
	  	</tbody>
   <?php else: ?>
   	<tr class="table_liste">
   	 <td colspan='5'>
   		Aucun résultat
   	 </td>
   	</tr>
   <?php endif; ?>
   </table>
	<br />
</div>
</div>
<?php echo $this->render("commun/box_bottom.phtml") ?>